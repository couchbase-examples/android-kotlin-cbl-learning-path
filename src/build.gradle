buildscript {
    ext {
        kotlin_version = '1.6.10'
        core_ktx = '1.7.0'
        compose_version = '1.1.1'
        activity_compose_version = '1.4.0'
        lifecyle_runtime_ktx_version = '2.4.1'
        koin_version = '3.1.5'
        android_materialdesign_version = '1.5.0'
        androidx_navigation_compose_version = '2.4.2'
        google_accompanist_version = '0.23.1'
        kotlinx_serialization_json = '1.3.2'
        constraints_compose_version = '1.0.0'
        annotation_experimental_version = '1.2.0'

        //couchbase
        couchbase_lite_version = '3.0.0'
    }

    repositories {
        maven {
            url "https://mobile.maven.couchbase.com/maven2/dev/"
        }
        google()
        mavenCentral()
    }
    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-serialization:$kotlin_version"
    }
}// Top-level build file where you can add configuration options common to all sub-projects/modules.

plugins {
    id "com.android.application" version '7.1.2' apply false
    id "com.android.library" version '7.1.2' apply false
    id "org.jetbrains.kotlin.android" version "1.6.10" apply false
    id "org.jetbrains.kotlin.jvm" version "1.6.20" apply false
    id "org.jetbrains.kotlin.plugin.serialization" version "1.6.10"
    id "com.github.johnrengelman.shadow" version "7.1.2" apply false
}

task clean(type: Delete) {
    delete rootProject.buildDir
}